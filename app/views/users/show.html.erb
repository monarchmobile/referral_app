  <!-- profile page -->
  <%= render'shared/notifications' %>
  <div class="contact_page">
    
    <div class="info_container">
      <%= render'shared/contact_emails', contact: @user %>
      <%= render'shared/contact_phone_numbers', contact: @user %>
      <%= render'shared/contact_addresses', contact: @user %>
    </div>

      
  </div>
  <%= render'shared/referral_links', user: @user %>

 
  


